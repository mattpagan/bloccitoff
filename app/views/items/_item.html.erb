<%= content_tag :div, class: 'item', id: "item-#{item.id}" do %>
  <div class="pull-left"><%= item.name %></div>
  <%= link_to [item], method: :delete, class: 'pull-right', remote: true do %>
    <span class="glyphicon glyphicon-ok"></span>
  <% end %>
<% end %>